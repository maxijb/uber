/* Renders the marker for locations' markers */

import {default as React} from 'react';

export default React.createClass({

  render: function() {
  	
    return (
    	<div className="map-icon icon-location2">
        </div>
    );
  }

});